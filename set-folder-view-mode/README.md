# Set Folder View Mode

## Description

Recursively sets the view mode for folders on a given path.
Doesn't seem to work.

## Running

- Run `set-folder-view-mode.py`.
- Run `python set-folder-view-mode.py` in the terminal.
