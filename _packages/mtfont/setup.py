from setuptools import setup, find_packages

setup(
  name = 'mtfont',
  version = '1.0.0',
  packages = find_packages(),
  description = 'A minimal Windows font loading helper',
  author = 'jesuscc1993',
)
