from setuptools import setup, find_packages

setup(
  name = 'mthltb',
  version = '1.0.0',
  packages = find_packages(),
  description = 'A minimal HowLongToBeat search client',
  author = 'jesuscc1993',
)
