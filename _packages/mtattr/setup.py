from setuptools import setup, find_packages

setup(
  name = 'mtattr',
  version = '1.0.0',
  packages = find_packages(),
  description = 'A minimal Windows file attribute helper',
  author = 'jesuscc1993',
)
