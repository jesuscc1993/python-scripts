# Replace Files With Dummies

## Description

Recursively replaces files matching a pattern with empty dummies.

## Requirements

- Having python installed.
- Running `pip install -r requirements.txt` to install the required dependencies.

## Running

- Run `replace_files_with_dummies.py`.
- Run `python replace_files_with_dummies.py` in the terminal.
